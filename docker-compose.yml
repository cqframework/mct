version: "3.9"
services:
  # frontend:
  #   build:
  #     context: frontend/
  #   ports:
  #     - 3000:80
  backend:
    build:
      context: java/
    ports:
      - 8088:8088
  cqf-ruler:
    image: alphora/cqf-ruler:vsm_draft_operation 
    ports:
      - 8080:8080